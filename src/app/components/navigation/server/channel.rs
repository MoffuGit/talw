use leptos::*;

use crate::entities::channel::Channel;

#[component]
pub fn Channel(channel: Channel) -> impl IntoView {
    view! {
        <div class="relative transition duration-200 ease-in-out delay-0 hover:bg-base-content">
        </div>
    }
}
